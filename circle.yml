## Customize the test machine
machine:

  timezone:
    Asia/Seoul # Set the timezone

  # Version of ruby to use
  python:
    version:
      3.6.0

## Customize dependencies
dependencies:
  pre:
    - pip install pygame